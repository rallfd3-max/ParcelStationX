import {describe,expect,it} from 'vitest';import {canAccessSettings,filterExceptions,filterParcelDetails,groupShelvesByZone,parcelFocusLocation,viewState} from './management';import type{ExceptionRecord,ParcelDetails,Shelf}from'@/types/api'
const detail={parcel:{id:7,trackingNo:'SF-001',courierCompany:'SF',customerId:1,shelfId:null,slotId:null,status:'IN_STOCK',arrivedAt:'2026-01-01',pickedUpAt:null,operatorId:1,remark:'',version:0},customer:{id:1,name:'张三',maskedMobile:'138****0000'},operator:'员工',shelfCode:null,slotCode:null,events:[],relocations:[],pickupCode:'778899'} satisfies ParcelDetails
const exception={id:1,parcelId:7,exceptionType:'DAMAGED',description:'外包装破损',status:'OPEN',createdBy:1,handledBy:null,createdAt:'2026-01-01',handledAt:null,resolution:null} satisfies ExceptionRecord
describe('management views',()=>{it('searches parcel tracking pickup and customer fields',()=>{expect(filterParcelDetails([detail],'7788')).toHaveLength(1);expect(filterParcelDetails([detail],'李四')).toHaveLength(0)});it('selects exceptions by search and filters',()=>{expect(filterExceptions([exception],'SF-','OPEN','DAMAGED',()=> 'SF-001')).toHaveLength(1);expect(filterExceptions([exception],'','RESOLVED','',()=> '')).toHaveLength(0)});it('groups arbitrary zones and naturally sorts shelves',()=>{const shelf=(id:number,code:string,zone:string)=>({id,shelfCode:code,zone,capacity:1,occupied:0,status:'ACTIVE'} as Shelf);expect(groupShelvesByZone([shelf(2,'E-10','E'),shelf(1,'F-01','F'),shelf(3,'E-02','E')])).toEqual([{zone:'E',shelves:[expect.objectContaining({shelfCode:'E-02'}),expect.objectContaining({shelfCode:'E-10'})]},{zone:'F',shelves:[expect.objectContaining({shelfCode:'F-01'})]}])});it('builds 2D and 3D focus navigation',()=>{expect(parcelFocusLocation(7)).toEqual({path:'/digital-twin',query:{parcelId:7}});expect(parcelFocusLocation(7,false).path).toBe('/warehouse')});it('allows only admins into settings',()=>{expect(canAccessSettings('ADMIN')).toBe(true);expect(canAccessSettings('STAFF')).toBe(false)});it('represents loading error empty and ready states',()=>{expect(viewState(true,'',0)).toBe('loading');expect(viewState(false,'boom',0)).toBe('error');expect(viewState(false,'',0)).toBe('empty');expect(viewState(false,'',2)).toBe('ready')})})
