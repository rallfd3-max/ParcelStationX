import type { PerspectiveCamera } from 'three'; import { Vector3 } from 'three'; import type { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js'; import type { ShelfLayout,ShelfSlot } from '@/types/api'; import { cameraFocus,slotWorldPosition } from './sceneMath'; import { sceneBounds } from './sceneBounds'
export class CameraController { private animation=0; constructor(private camera:PerspectiveCamera,private controls:OrbitControls,private layouts:()=>ShelfLayout[],private slots:()=>ShelfSlot[]){} focusSlot(id:number){const s=this.slots().find(x=>x.id===id),l=s&&this.layouts().find(x=>x.shelfId===s.shelfId);if(s&&l)this.animate(cameraFocus(l,slotWorldPosition(l,s)))} focusShelf(id:number){const l=this.layouts().find(x=>x.shelfId===id);if(l)this.animate(cameraFocus(l,{x:l.positionX,y:l.height/2,z:l.positionZ}))} focusParcel(slotId:number){this.focusSlot(slotId)} resetCamera(){const b=sceneBounds(this.layouts()),d=Math.max(b.spanX,b.spanZ)*.85;this.animate({position:{x:b.centerX+d*.7,y:b.maxHeight+d*.55,z:b.centerZ+d},target:{x:b.centerX,y:b.maxHeight*.4,z:b.centerZ}})} topView(){const b=sceneBounds(this.layouts()),d=Math.max(b.spanX,b.spanZ);this.animate({position:{x:b.centerX,y:b.maxHeight+d,z:b.centerZ+.01},target:{x:b.centerX,y:0,z:b.centerZ}})} frontView(){const b=sceneBounds(this.layouts()),d=Math.max(b.spanX,b.spanZ);this.animate({position:{x:b.centerX,y:b.maxHeight*.8,z:b.centerZ+d},target:{x:b.centerX,y:b.maxHeight*.4,z:b.centerZ}})} cancelAnimation(){cancelAnimationFrame(this.animation)} private animate(view:{position:{x:number;y:number;z:number};target:{x:number;y:number;z:number}}){this.cancelAnimation();const from=this.camera.position.clone(),targetFrom=this.controls.target.clone(),start=performance.now(),to=new Vector3(view.position.x,view.position.y,view.position.z),target=new Vector3(view.target.x,view.target.y,view.target.z);const tick=(now:number)=>{const t=Math.min(1,(now-start)/800),e=1-Math.pow(1-t,3);this.camera.position.lerpVectors(from,to,e);this.controls.target.lerpVectors(targetFrom,target,e);this.controls.update();if(t<1)this.animation=requestAnimationFrame(tick)};this.animation=requestAnimationFrame(tick)} }
