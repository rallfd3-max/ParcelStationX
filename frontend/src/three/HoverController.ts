import { Raycaster,Vector2 } from 'three'
import type { Camera,Object3D } from 'three'
import type { SceneIndex } from './SceneIndex'
export interface ParcelHover { parcelId:number; x:number; y:number }
export class HoverController { private raycaster=new Raycaster();private pointer=new Vector2();constructor(private element:HTMLElement,private camera:Camera,private index:SceneIndex,private disabled:()=>boolean,private change:(hover:ParcelHover|null)=>void){element.addEventListener('pointermove',this.move);element.addEventListener('pointerleave',this.leave)}private move=(event:PointerEvent)=>{if(this.disabled()){this.change(null);return}const rect=this.element.getBoundingClientRect();this.pointer.set((event.clientX-rect.left)/rect.width*2-1,-(event.clientY-rect.top)/rect.height*2+1);this.raycaster.setFromCamera(this.pointer,this.camera);const hit=this.raycaster.intersectObjects([...this.index.parcels.values()],true)[0]?.object,id=this.index.parcelId(hit??null);this.change(id?{parcelId:id,x:event.clientX-rect.left+14,y:event.clientY-rect.top+14}:null)};private leave=()=>this.change(null);dispose(){this.element.removeEventListener('pointermove',this.move);this.element.removeEventListener('pointerleave',this.leave);this.change(null)}}
