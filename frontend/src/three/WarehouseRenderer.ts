import { PerspectiveCamera,WebGLRenderer } from 'three'
export class WarehouseRenderer { readonly renderer:WebGLRenderer; readonly camera:PerspectiveCamera; constructor(container:HTMLElement){this.renderer=new WebGLRenderer({antialias:true});this.renderer.setPixelRatio(Math.min(devicePixelRatio,2));this.renderer.setSize(container.clientWidth,container.clientHeight);container.appendChild(this.renderer.domElement);this.camera=new PerspectiveCamera(48,container.clientWidth/container.clientHeight,.1,200);this.camera.position.set(8,7,10)} resize(w:number,h:number){this.camera.aspect=w/h;this.camera.updateProjectionMatrix();this.renderer.setSize(w,h)} dispose(){this.renderer.dispose();this.renderer.domElement.remove()} }
